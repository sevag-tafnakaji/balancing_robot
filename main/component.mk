#
# "main" pseudo-component makefile.
#
# (Uses default behaviour of compiling all source files in directory, adding 'include' to include path.)

COMPONENT_EMBED_TXTFILES := resources/ca.pem
COMPONENT_EMBED_TXTFILES += resources/server.pem
COMPONENT_EMBED_TXTFILES += resources/server.key
